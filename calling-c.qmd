# Calling C from R

::: {.callout-warning}
Placeholder. Planned: `.Call` and `.External`; routine registration
(`R_registerRoutines`, `R_CallMethodDef`, `useDynLib`); the contract your C
function signs up to (main thread only, may longjmp on error, arguments are
borrowed references, inputs must not be modified); minimal package skeleton
and fast iteration workflow.
:::
