# Object oriented programming

{{< include _setup.qmd >}}

At the C level, R's object systems are thin: S3 is just a `class` attribute
plus conventions for dispatch, and S4 adds a bit on the object header
(making it an `S4SXP`) plus slot attributes. (`ANYSXP` is used internally
in S4 class definitions to flag that any SEXP is acceptable.)

## Object testing

```{r}
#| results: asis
#| echo: false
render_entries("oo", "Object testing")
```

## S3

```{r}
#| results: asis
#| echo: false
render_entries("oo", "S3")
```

## S4 (`S4SXP`)

```{r}
#| results: asis
#| echo: false
render_entries("oo", "S4 (`S4SXP`)")
```

### Slots

```{r}
#| results: asis
#| echo: false
render_entries("oo", "Slots")
```
